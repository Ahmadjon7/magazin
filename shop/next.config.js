/** @type {import('next').NextConfig} */
const nextConfig = 
{
    images: {
        domains: ['fakestoreapi.com' , 'www.google.com'], // Add any other domains

      },
}

module.exports = nextConfig
